_schema-version: '3.1'
ID: DigTrialEndpoint
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npx cds build --production
modules:
  - name: DigTrialEndpoint-srv
    type: nodejs
    path: gen/srv
    parameters:
      disk-quota: 256M
      memory: 64M
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: DigTrialEndpoint-db

  - name: DigTrialEndpoint-db-deployer
    type: hdb
    path: gen/db
    parameters:
      disk-quota: 256M
      memory: 64M
      buildpack: nodejs_buildpack
    requires:
      - name: DigTrialEndpoint-db

resources:
  - name: DigTrialEndpoint-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
