_schema-version: "3.1"
ID: demoCAP
description: A simple CAP project.
version: 1.0.0
modules:
  - name: demoCAP-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: demoCAP-db
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      memory: 64M
    build-parameters:
      builder: npm
  - name: demoCAP-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: demoCAP-db
    parameters:
      buildpack: nodejs_buildpack
      memory: 64M
  - name: demoCAP-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: demoCAP-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - democap.zip
          name: democap
          target-path: resources/
  - name: democap
    type: html5
    path: app/democap
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
resources:
  - name: demoCAP-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: demoCAP-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: demoCAP-html5-srv
      service-plan: app-host
  - name: demoCAP-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: false
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: demoCAP-destination-service
      service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npx cds build --production
